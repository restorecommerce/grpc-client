{
  "name": "@restorecommerce/grpc-client",
  "version": "0.1.0",
  "description": "gRPC client",
  "main": "./lib/index.js",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/grpc-client.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "gRPC",
    "client"
  ],
  "typings": "lib/index.d.ts",
  "dependencies": {
    "@restorecommerce/logger": "^0.1.3",
    "async-retry": "^1.2.1",
    "co": "^4.6.0",
    "grpc": "1.10.1",
    "lodash": "^4.17.5",
    "random-js": "^1.0.8"
  },
  "devDependencies": {
    "@types/long": "^3.0.31",
    "@types/mocha": "^5.0.0",
    "@types/node": "^9.6.5",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.4",
    "mocha": "^5.1.0",
    "should": "^13.2.1",
    "sleep": "^5.1.1",
    "tslint": "^5.9.1",
    "typescript": "^2.8.1"
  },
  "scripts": {
    "lint": "tslint './src/**/*.ts' './test/*.ts'",
    "pretest": "npm run tsctests",
    "tsctests": "tsc test/*.ts --module commonjs -target ES6",
    "test": "npm run lint && npm run mocha",
    "test-debug": "mocha -R spec ./test/*_test.js --inspect-brk --exit",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec ./test/*_test.js --exit",
    "mocha": "mocha -R spec ./test/*_test.js --exit",
    "postinstall": "tsc -d"
  },
  "engines": {
    "node": ">= 9.2.0"
  }
}
